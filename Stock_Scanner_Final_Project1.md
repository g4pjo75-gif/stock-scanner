# 프로젝트명: 글로벌 이평선 대순환 & 모멘텀 스캐너 (JP/US)

## AI 개발 가이드
"너는 이제 시니어 풀스택 개발자야. 아래 설계안을 바탕으로, 특히 수천 개의 미국 주식 데이터를 효율적으로 필터링하여 프론트엔드 리스트에 뿌려주는
 'FastAPI 백엔드 스캐너' 로직과, 국가 전환 기능이 포함된 'Next.js 프론트엔드 레이아웃' 코드를 작성해줘."

## 1. 데이터 아키텍처 및 최적화
- **Market Support**: 일본(JP), 미국(US) 시장 선택 기능 제공.
- **Data Pipeline**: 
    - 백엔드(Python/FastAPI)에서 일별 데이터를 수집하여 이평선 상태(고지로 스테이지 1~6)를 사전 계산.
    - Redis 캐시를 사용하여 프론트엔드 요청 시 사전 계산된 '스테이지별 종목 리스트'만 즉시 반환.
- **Performance**: 전 종목 스캔 시 발생하는 과부하를 막기 위해 시가총액 상위 종목 및 거래대금 필터 적용.

## 2. 대시보드 UI/UX 설계
- **Market Toggle**: 상단 국기 아이콘으로 JP/US 시장 즉시 전환.
- **Main List View**:
    - **Pick 1 (Gojiro Stage 6)**: 5 > 40 > 20 이평선 배열 발생 종목 (상승 반전 포착).
    - **Pick 2 (Kullamägi Momentum)**: 3개월 내 30%+ 상승주 중 횡보 구간 돌파 시도 종목.
    - **Pick 3 (Volume Spike)**: 평균 거래량 대비 300% 이상 폭증하며 정배열 진입한 종목.
- **Chart Layout**: 종목 클릭 시 TradingView 차트 팝업. 5/20/40(SMA), 10/20/50(EMA) 지표 자동 세팅.

## 3. 핵심 알고리즘 코드 (Python 예시)
- `check_gojiro_stage(df)`: OHLC 데이터를 받아 현재 이평선 배열에 따른 스테이지(1~6) 반환.
- `check_kullamagi_setup(df)`: 3개월 모멘텀 및 최근 n일간 변동성 수축(VCP) 여부 판단.
- `calculate_position_size(total_capital, stop_loss_price)`: 영상의 '1% 리스크 관리' 로직 구현.
- 정밀 필터 (승률 최적화)
  거래량 필터: 20일 평균 대비 당일 거래량 200% 이상.
  이격도 필터: 주가가 10일 EMA에서 ADR(평균 등락폭) 이상 벌어지지 않았을 때만 진입 (추격 매수 방지).
 
## 4. Daily Reporting & Archiving System (직장인 맞춤형)

### A. 자동 스케줄러 (Cron Job)
- 매일 22:00(KST) 전 종목 스캔 실행.
- 분석 결과는 `daily_reports` 테이블에 JSON 형태로 저장 (날짜별 조회 가능).

### B. 리포트 알고리즘
- **Step 1 (Filtering)**: 거래대금 하위 20% 및 동전주 제외.
- **Step 2 (Scoring)**: 
    - 고지로 스테이지 6(단장중) 해당 여부.
    - 쿨라메기 VCP(최근 5일 변동성 < 10일 변동성) 수축 여부.
- **Step 3 (Ranking)**: 모멘텀 강도 순으로 시장별 Top 10 추출.

### C. 사용자 인터페이스 (UI)
- **캘린더 뷰**: 과거 날짜 클릭 시 해당 날짜의 리포트와 어드바이스 복기 가능.
- **Advice Badge**: 각 종목 옆에 [매수], [매도], [홀딩] 배지 표시.
- **Position Calculator**: 어드바이스 옆에 '1% 손절 원칙'에 따른 주식 수 자동 계산기 배치.